{
  "name": "uner",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"cd front && yarn dev\" \"cd nest && yarn dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}